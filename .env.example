# Optional: pin the tenant if `az login` has access to more than one.
TENANT_ID=

# Optional: set these to authenticate as a service principal instead of
# your signed-in `az login` identity (e.g. for unattended/CI runs).
# Leave blank to use DefaultAzureCredential.
CLIENT_ID=
CLIENT_SECRET=
